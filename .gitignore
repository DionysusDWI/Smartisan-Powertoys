# ============================================================================
#  Smartisan Powertoys —— 开发预览版 .gitignore
#
#  ★ 本仓库是【源码 + 研究文档】的公开镜像，**不入库**三类东西：
#    ① 体积巨大的本地资产（工具链 / 反编译语料 / 抓包 / 图片）
#    ② 构建产物与 IDE 缓存（谁都能自己生成）
#    ③ **本机私有状态**（device 序列号、绝对路径、AI 助手会话、授权文件）
#
#  ⚠️ 前三类里 ③ 是**安全相关**的，改动这一条要格外小心。
# ============================================================================

# ---------------------------------------------------------------- 便携工具链 / 本地资源
#  3102 MB JDK+SDK、838 MB 反编译语料、457 MB 原始抓包、170 MB Python venv
#  ★ 这些都能按 README 的说明自行准备；直接传上去只会把仓库撑爆。
/toolchain/
/vendor/
/.resource/
/.venv/
/.tmp/
/archive/

# ---------------------------------------------------------------- 构建产物
build/
.gradle/
.kotlin/
*.apk
*.aar
*.idsig
!docs/**/*.apk

# ---------------------------------------------------------------- IDE / 系统
.idea/
*.iml
.DS_Store
Thumbs.db
*.hprof

# ---------------------------------------------------------------- ★ 本机私有状态（安全）
#  local.properties 里是**本机 SDK 绝对路径**（含用户名）—— 绝不能上传
local.properties
**/local.properties
#  调试用的 dump / 抓包 / 日志：含设备序列号、Wi-Fi 地址、SharedPreferences 原文
/.ref/*
!/.ref/Search-Results/
#  ⚠️ Search-Results/raw 里是**第三方文档原件**（TI 数据手册 PDF、AOSP 源码、厂商内核驱动），
#     体积大且**不是本项目的产出**，公开镜像里只保留我们自己整理的 `.md` 结论。
#     （`INDEX.md` 里记着每个原件的 URL —— 需要时按链接自取。）
/.ref/Search-Results/raw/
/docs/_workflow_cache/
/__CC-DSH-Bridge__/
/refs/
/handover/
#  小米迁移工具包（二进制封装件）—— 属于交付物，不进源码镜像
xiaomi_migration_kit_*.tar.gz
xiaomi_migration_kit_*.sha256
*.keystore
*.jks
*.log

# ---------------------------------------------------------------- 内部开发过程材料
#  ★ 这些是**多 agent 协作 + 进度追踪**的过程文件（含本机路径、会话规模诊断、
#    逐轮纠错留痕）。它们对继续开发**极其有用**，但属于**内部工作记录**，
#    不是这个公开镜像要发布的内容 —— 公开版只发布**结论**（.paper/ 与 docs/）。
/PROGRESS-STATE.MD
/CLAUDE.md
/.paper/plans/
#  ⚠️ `.paper/CHANGELOG.md` 是**逐轮留痕的流水账**（267 KB），属过程而非结论，
#     与上面三类同理。
#  ★ 2026-09-15 更正：本文件此前写着「由发布流程排除，**不是**靠这里的规则」——
#     而 GitHub 上的 `Smartisan-Powertoys` **就是**那个发布镜像，
#     于是注释说的和实际入库的内容**对不上**（文件在库里，规则里却没有它）。
#     ⇒ 现在改成**真的用规则排除**，注释与事实一致。
#     要恢复入库：删掉下面这行 ＋ `git add -f .paper/CHANGELOG.md`。
/.paper/CHANGELOG.md

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
