# 贡献指南

这个项目是在**一部真机上**长出来的，所以它的工程习惯跟一般开源项目略有不同。
动手前请先读这三节。

---

## 一、★★ 最重要的一条：**结论必须带着证据**

本项目**不禁止**你写"我猜"，但**禁止**把猜写成实测。

每条数据/结论都要能回答"**它是怎么来的**"，来源只有这五类：

| 标注 | 含义 |
|---|---|
| **实测** | 在这台设备上真的跑过、有 logcat / 截图 / 文件为证 |
| **反编译** | 从 APK / 系统镜像里读出来的 |
| **交接包** | 来自 `handover/` 的一手材料 |
| **网络** | 外部资料（★ **必须**归档到 `.ref/Search-Results/` 并记 URL） |
| **推断** | ★ **必须显式写"推断"** —— 这是最容易出错的类别 |

> **实践**：写"提升了 57.9%"之前，先想清楚**这个数字是在什么条件下测的**。
> 本项目已经因为"合成基准的收益 ≠ 真实场景的收益"纠正过一次产品定位。

---

## 二、★★ 推翻旧结论：**保留原文 ＋ 标注勘误**，不要直接删

工作区纪律：

> **结论被推翻时保留原判定并标注勘误** —— 避免重复踩坑。

例：`docs` 与 `.paper` 里有些段落被划掉但**没删**，后面跟着一行"★ 已推翻，原因：…"。
**这是故意的。** 直接删掉会让后来的人（包括几个月后的你自己）重新走一遍同一条错路。

---

## 三、★★★ "测不出来"是一个**合法结论**

如果实验做完、样本够、但**效应量不足**，正确做法是：

* 写下「**不可分辨**」并给出数据
* **不要**为了得到结论而降低判据，**不要**把噪声当信号

同理，**宁可不显示，也不要显示一个猜的数**。见 README「两条工程习惯」。

---

## 四、动手改代码

### 先写计划书

任何非平凡改动，先在 `.paper/plans/<任务名>.md` 写计划书
（**目标 / 当前状态 / 步骤清单 / 关键数据 / 下一步**），**边做边更新**。
理由很实际：这是个长周期项目，**计划书是上下文被压缩后恢复现场的唯一依靠**。

### 加一个 mod

`mod-hello/` 就是模板。一个 mod 只需要：

1. 一个 `Service`，在 manifest 里声明 `android:process`（★ 见下）
2. 一个 `<intent-filter>` 收 `com.shware.mode.action.MOD`
3. 若干 `MOD_*` meta-data 申报（ID / 名称 / 描述 / 目标屏 / 触摸模式 / 分类 / 图标…，★ 都可选，不写有合理默认）

★ **加新 mod，宿主界面零改动** —— 分类分组与图标都由申报驱动，宿主 Kotlin 一行不用动。

### ★★ 一个 mod = 一个进程

同一 mod 的**所有组件**（Service / Activity / Receiver）必须声明**同一个**
`android:process`。否则：

* 静态对象**不共享**（会是两份副本）
* 典型症状：设置界面操作的是**另一个实例**；通知栏按钮"点了没反应"

### 构建与部署

```bash
bash scripts/build.sh --list
bash scripts/build.sh mode-all
bash scripts/deploy.sh mode-all        # ★ 部署会打印需要【人工点击】的授权待办，别忽略
```

### 提交前自检

```bash
python scripts/check_log_tags.py       # 校验脚本引用的 logcat TAG 仍存在于源码
python scripts/check_doc_links.py      # 校验文档内部链接
```

> ★ 这两个脚本都是**结构性的**：它们防的是同一类病 ——
> **改了实现，而用来验证它的工具/文档没跟着改**（于是工具开始说谎，而且看起来一切正常）。

---

## 五、不要提交的东西

`.gitignore` 已经处理了大部分，这里只强调**安全相关**的：

* ❌ `local.properties`（**含本机绝对路径与用户名**）
* ❌ `*.keystore` / `*.jks`
* ❌ 任何 **API Key**（本项目要求 Key 只存在于**环境变量或本机设置**里）
* ❌ 设备 dump / 抓包（**含设备序列号、Wi-Fi 地址**）
* ❌ `.ref/Search-Results/raw/`（**第三方文档原件**，体积大且不是本项目产出）
